apiVersion: v1
kind: Secret
metadata:
  name: shoprite-mcp-secrets
  namespace: shoprite-mcp
  labels:
    app.kubernetes.io/name: shoprite-mcp
    app.kubernetes.io/component: secrets
type: Opaque
# Values must be base64-encoded.
# Generate with: echo -n 'your-value' | base64
stringData:
  REDIS_PASSWORD: "changeme"   # Override in production via sealed-secrets / external-secrets
